overwrite: true
schema: "http://localhost:3000/graphql"
documents: "src/**/*.gql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript-common"
      - "typescript-react-apollo"
  ./graphql.schema.json:
    plugins:
      - "introspection"
